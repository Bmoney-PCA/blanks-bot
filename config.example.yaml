printavo:
  email: you@purechoiceapparel.com      # email tied to your Printavo API token
  token: PRINTAVO_API_TOKEN             # My Account > API  (paste yours here; this file is gitignored)
  needs_blanks_status_ids:              # status(es) that hold orders needing blanks
    - "0000"                            # run --list-statuses to find real ids
  received_status_ids:                  # status(es) that mean "Received Garments"
    - "0000"                            # the SUBTRACT trigger

sku_map_path: sku_map.csv
output_dir: .                           # where on_hand.csv / ledger.csv / tracker.html live
